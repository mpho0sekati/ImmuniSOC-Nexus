server.port=8081
server.servlet.context-path=

# Authentication secret for proxy-backend handshake
auth.handshake.secret=${HANDSHAKE_SECRET_TOKEN:your-default-secret-key-here}

# Security configuration
management.endpoints.web.exposure.include=health,info